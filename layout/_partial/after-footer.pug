script.
  window.config = {
    url_root: '#{theme.root}',
    meta_path: '#{(theme.search || {}).path || undefined}',
  };

//- theme modules
| !{ js('js/theme/back-to-top') }
| !{ js('js/theme/clipboard') }
| !{ js('js/theme/loading') }
| !{ js('js/theme/navbar') }

if theme.search.enable
  | !{ js('js/theme/search') }
if theme.toc.enable
  | !{ js('js/theme/toc') }
if theme.smooth_corner
  | !{ js('js/theme/smooth-corners') }

script.
  window.onload = function () {
    for (const moduleName in Theme) {
      const module = Theme[moduleName];
      module.register();
    }
  };

| !{ partial('_partial/umami/script') }